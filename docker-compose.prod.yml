services:
  caddy:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: https://ensinadev.com.br/api
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./deploy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - backend

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - ./backend/.env.prod
    volumes:
      - uploads:/app/uploads
    depends_on:
      - db
    expose:
      - "3001"

  db:
    image: postgres:16-alpine
    restart: unless-stopped
    env_file:
      - ./backend/.env.prod
    volumes:
      - pgdata:/var/lib/postgresql/data

  # Executor isolado dos desafios de código. É o único serviço com acesso ao
  # Docker: sobe um container efêmero por submissão. O backend fala com ele pela
  # rede interna (RUNNER_URL padrão http://runner:8080). As imagens de execução
  # desafio-js/desafio-python precisam existir no host (rode runner/build-images.sh).
  runner:
    build:
      context: ./runner
    restart: unless-stopped
    environment:
      - MAX_CONCURRENT=2
      # Camada extra de isolamento: com o gVisor (runsc) instalado no host, cada
      # submissão roda sobre um kernel próprio, e não sobre o kernel do host.
      # Vazio = runtime padrão do Docker, que é o comportamento de sempre.
      - RUNNER_RUNTIME=${RUNNER_RUNTIME:-}
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock

  # Laboratórios de Linux: um container com shell por sessão de aluno.
  # Fala com um daemon Docker SEPARADO, com userns-remap ligado, para que o root
  # de dentro do lab seja um UID sem privilégio para o kernel do host. Ver
  # labs/README.md para preparar esse daemon no servidor.
  labs:
    build:
      context: ./labs
    restart: unless-stopped
    environment:
      - MAX_SESSOES=${LAB_MAX_SESSOES:-20}
      - LAB_TTL_MINUTOS=${LAB_TTL_MINUTOS:-45}
      - LAB_DOCKER_HOST=unix:///var/run/docker-labs.sock
    volumes:
      - /var/run/docker-labs.sock:/var/run/docker-labs.sock

volumes:
  pgdata:
  caddy_data:
  caddy_config:
  uploads:
